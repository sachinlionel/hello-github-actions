name: env

on:
  push:
  workflow_dispatch:
    inputs:
      log_Level:
        description: 'Log level'
        required: true
        default: 'warning'


jobs:
  job1:
    env:
      LOG_LEVEL: ${{ github.event.inputs.LOG_LEVEL }}
    runs-on: ubuntu-latest
    steps:
      - name: setenv
        run: echo ${{ toJson(github.event.env) }}
      - name: Print input
        run: echo "The log level is $LOG_LEVEL"