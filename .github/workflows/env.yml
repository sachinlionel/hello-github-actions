name: env

on:
  push:
  workflow_dispatch:
    inputs:
      log_Level:
        description: 'Log level'
        required: true
        default: 'warning'


jobs:
  job1:
    env:
      LOG_LEVEL: ${{ github.event.inputs.log_Level }}
    runs-on: ubuntu-latest
    steps:
      - name: setenv
        if: ${{ env.LOG_LEVEL == '' }}
        run: export LOG_LEVEL=test
      - name: Print input
        run: echo "The log level is $LOG_LEVEL"