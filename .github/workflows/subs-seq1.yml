name: sub-seq1
on:
  push:
    paths:
      - action-a/**
      - config/**
    # AND
    branches:
      - main
  # OR
  workflow_run:
    workflows: ["wf-main"]
    types:
      - completed

jobs:
  test:
    name: echo test
    runs-on: ubuntu-latest
    steps:
      - name: test title
        run: echo testmain