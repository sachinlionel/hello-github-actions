name: sub-seq1
on:
#  push:
#    paths:
#      - action-a/**
#      - config/**
#    # AND
#    branches:
#      - main
  # AND
  workflow_run:
    paths:
      - action-a/**
      - config/**
    branches:
      - main
    workflows: ["wf-main"]
    types:
      - completed

jobs:
  test:
    name: echo test
    runs-on: ubuntu-latest
    steps:
       - name: Checkout the code
         uses: actions/checkout@v2
         with:
            ref: ${{ github.event.workflow_run.head_sha }}

       - name: Set PP_IMAGE_TAG and PP_IMAGE env used in the next steps
         run: |
          PP_IMAGE_TAG=$(${GITHUB_WORKSPACE}/script/version ${PP_SERVICE}_) &&
          echo ${PP_IMAGE_TAG}